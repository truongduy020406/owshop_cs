import React from 'react';

const AuthClerkLayout = ({ children }) => {
  return (
    <div className="flex h-screen justify-center items-center">{children}</div>
  );
};

export default AuthClerkLayout;
